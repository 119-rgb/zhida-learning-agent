# 项目协作规则

## 代码复用与注释

- 开发或改造功能前，先检查现有实现，尽量复用适用的业务服务、组件、工具方法和测试基础设施，避免重复实现相同逻辑。确需调整时优先扩展现有代码；复用应保持职责清晰，不为复用而引入不必要的耦合或过度抽象。
- 新增或修改代码时，必须补充清晰的中文注释，帮助学习者理解代码。类和关键方法说明职责、输入输出及必要约束；复杂流程、业务规则、权限校验、状态流转、事务、幂等、并发控制和异常处理说明设计原因及边界。简单代码无需逐行复述，修改逻辑时同步更新相关注释，保证注释与实际行为一致。

## 隐私与公网发布

- 禁止将用户的任何私密信息上传或发布到 GitHub 或其他公网平台，包括公开仓库、提交记录、Pull Request、Issue、评论、日志和构建产物。
- 私密信息包括真实姓名、个人邮箱、手机号、身份证件、住址、简历中的私人资料、可识别用户身份的本地路径，以及 API Key、密码、Token、私钥、Cookie、数据库凭据和其他认证信息。
- 任何推送或公网发布前，必须检查待发布文件、附件、生成产物以及将被推送的全部 Git 历史，尤其是提交作者和提交者的姓名、邮箱；不能只检查当前文件或最新提交。
- Git 提交身份使用不含真实姓名的昵称和 GitHub noreply 邮箱；配置或示例只使用环境变量、空值或明确的占位符。私密文件应排除在版本控制之外；忽略规则不能替代对已跟踪文件和历史记录的检查。
- 发现疑似私密信息时停止相关推送或发布，报告位置并对内容脱敏，不在对话、日志或其他公网内容中重复完整密钥。先处理泄露风险并重新检查，通过后才能发布。
- 一般性的“提交”“推送”“发布项目”授权不代表允许披露私密信息。未经用户明确授权，不得为清理历史擅自重写远端历史或强制推送；已公开的密钥还需及时撤销或轮换。

## 分阶段审查与注释

- 每个业务模块或重要开发阶段完成后进行独立 review，修复重要问题，再对修复范围复查；审查范围、问题、修复及实际验证记录在 docs/reviews/。
- 代码注释优先解释业务约束、权限依据、合法状态、事务边界、幂等与并发控制；必须与实现一致，不以注释声称尚未完成的功能或验证。
- 自动测试、真实 MySQL、真实模型和浏览器验收分别记录。审查不替代测试，也不替代发布前完整隐私检查。
